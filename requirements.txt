# MyClover.Tech.SentryLog v6.0 - Requirements
# Core dependencies (required)
flask>=3.0.0
pyyaml>=6.0

# Phase 2: Windows Event Log (optional -- install as needed)
# Local Windows Event Log collection (Windows only):
#   pip install pywin32
# Remote Windows Event Log collection (any OS):
#   pip install pywinrm

# Phase 3: Security API connectors (optional -- install as needed)
# Required for CrowdStrike, SentinelOne, Defender, Sophos, Cortex XDR, Generic API:
#   pip install requests

# Phase 4: Correlation + Compliance (no extra dependencies)
# Phase 5: Email notifications (built-in smtplib), scheduled reports, log export
# Phase 6: File tailing, log forwarding, API keys (no extra dependencies)
#
# All Phase 4-6 features use Python stdlib + SQLite. No extra installs needed.
